#PROGRAMA PARA MOSTRAR EN PATALLA LOS DECODIFICADORES "NOS VEMOS EN LA FIESTA CACHIMBO"
msg=""
import os

msg=str(os.sys.argv[1])

for letra in msg:
    if (letra=="J"):
        print("NOS")
    if(letra=="H"):
        print("VEMOS EN LA")
    if(letra=="O"):
        print("FIESTA")
    if(letra=="N"):
        print("CACHIMBO")
    #fin_if
#fin_for
